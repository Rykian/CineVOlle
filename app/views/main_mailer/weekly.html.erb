<h1>CineVOlle</h1>

<p>
  <%=t '.intro' %>
  <ul>
    <% @current_week.each do |movie| %>
      <li>
        <strong><%= movie.title %></strong> :
        <%= movie.sessions.where(date: current_theater_week).pluck(:date).map {|d| l d, format: :short}.to_sentence %>
      </li>
    <% end %>
  </ul>
</p>

<p style="font-size: 10px; text-align: center;"><%= link_to 'Se désinscrire', unsubscribe_url(email: @user.email) %></p>
